# Caligraphia Web/iOS/Android Parity Tracker

Last reviewed: 2026-06-13

| Feature ID | Product Area | User Flow | Web Routes / Components | API Routes | Prisma Models | Web Status | iOS Status | Android Status | Parity Status | UX Variance | Contract | Acceptance Criteria | Evidence / Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| auth.magic-link | Auth | User requests and verifies a magic link, then lands in an authenticated session. | `/login`, `/signup`, `AuthGuard`, `providers` | `/api/auth/send-magic-link`, `/api/auth/verify-magic-link`, `/api/auth/me`, `/api/auth/logout` | `User` | partial | in progress | in progress | blocked | Native should use universal links/app links; web uses email link route. | v1 | User can request link, verify token, persist session, logout. | iOS and Android have dev auth panels using existing cookie routes. Production universal links, Android app links, and signup handoff remain. |
| auth.signup | Auth | New user claims username and optional nom de plume after magic link. | `/signup`, `NoTextInputWrapper` | `/api/auth/signup` | `User` | live | not started | not started | behind | Username remains the only typed identity field. | v1 | Signup creates user, sets session cookie, rejects duplicate username. | Native signup and optional avatar upload still need a shared flow. |
| compose.canvas | Writing | User writes a letter by hand, waits minimum duration, and submits. | `/post/new`, `CanvasDraw`, `ink-engine` | `/api/posts` | `Post` | live | in progress | in progress | behind | Native may use platform drawing engines, but shared feeds/details must display canonical `imageUrl`/proxied `finalImageUrl`. | v2-native | Letter has drawing provenance, rendered image, duration validation, paper/ink metadata, OCR, and feed/inbox visibility. | Brush palette expanded; addressed canvas/native letters become private; iOS and Android authenticated send paths are wired. Recipient-aware ComposeFlow and physical device writing QA remain. |
| compose.envelope | Writing | User seals letter in envelope with optional signature/wax data. | `/post/new`, `LetterEnvelope` | `/api/posts` | `Post.envelopeData`, `Post.signatureData` | live | planned | planned | behind | Native envelope can use platform animation but must store compatible envelope/signature data. | v1 | Envelope preview appears in feed; opening reveals letter. | Needs native data-shape fixture. |
| compose.photo | Writing | User uploads a photo letter and server runs OCR/review gate. | `/post/new`, `PhotoUpload` | `/api/posts` multipart | `Post` | live | in progress | in progress | behind | Native can use PhotosUI/Photo Picker/camera instead of browser file input. | v1 | Upload stores photo, runs OCR, marks review when no handwriting found. | iOS and Android multipart photo-letter send paths are wired; camera capture and polished share-in flows remain. |
| mobile.share-import | Mobile Sharing | User imports images from gallery or shares image drawings into the mobile app; user can share exported letters out. | Web equivalent is photo upload/download/share by browser. | `/api/posts` multipart or native drawing payload | `Post` | partial | in progress | in progress | acceptable variance | iOS uses PhotosPicker/ShareLink; Android uses Photo Picker/ACTION_SEND/FileProvider. Direct share-in extension on iOS is separate work. | v1 | Import image, preview it, share exported PNG, preserve no-typed-content rule. | iOS and Android can import/share images and submit imported images as photo letters. iOS Share Extension and Android recipient direct-share shortcuts remain. |
| letters.postbox | Letters | User browses public letters in envelope/gallery/table modes. | `/`, `Feed`, `EnvelopeCard`, `PaperTableView` | `/api/posts` | `Post`, `User`, `PostInteraction`, `Comment`, `Scratch` | live | in progress | in progress | behind | iPhone/Android may use cards first; iPad should keep tabletop feel. | v1 | Public feed paginates, excludes private/review/deleted/request asks, opens detail. | Stable post DTO/image proxy added; iOS and Android basic Postbox lists load authenticated data. |
| letters.inbox | Letters | User receives private letters addressed to them. | `/inbox` | `/api/posts/inbox` | `Post.recipientId`, `Post.isPrivate`, `Post.deliverAt` | live | in progress | in progress | behind | Native list is natural; privacy behavior must match. | v1 | Inbox shows arrived private letters, respects slow delivery. | Shared access helper and image proxy enforce private/slow-delivery rules; iOS and Android basic Inbox lists load authenticated data. |
| letters.detail | Letters | User opens a letter, stamps it, replies/postscripts, scratches, replays, or deletes own post. | `/post/[id]`, `StampButton`, `Postscripts`, `ScratchOverlay`, `DrawingReplay` | `/api/posts/:id`, `/stamp`, `/comments`, `/scratch`, `/scratches` | `Post`, `PostInteraction`, `Comment`, `Scratch`, `Marginalium` | live | in progress | in progress | behind | Native should use platform image zoom and PencilKit/Canvas for postscripts/scratch. | v1 | Detail enforces access, displays image/metadata, supports reactions and handwritten additions. | iOS and Android have basic read-only detail. Detail actions, replay, delete, and handwritten side content remain native gaps. Detail/side endpoints share access checks; web replay is hidden for native-origin PencilKit posts. |
| social.profile | Social | User views another desk, follows, writes, reads guestbook. | `/users/[id]` | `/api/users/:id`, `/follow`, `/posts`, `/guestbook` | `User`, `UserFollow`, `GuestbookEntry`, `Post` | live | not started | not started | behind | iPad can use split view profile + letters. | v1 | Profile shows stats, follow state, send-letter entry, guestbook, posts. | Guestbook media now uses authenticated proxy URLs; profile DTO fixtures and native UI remain. |
| social.search | Social | User finds people for addressing/request flows. | `UserSearch`, `/requests` | `/api/users/search`, `/api/users/prominent` | `User` | live | not started | not started | behind | Search is typed only for finding users, not composing content. | v1 | Results include id, username, nom de plume; selection drives compose context. | Keep no-text-content distinction clear. |
| exchange.letter | Letter Exchange | Stranger pairing creates reciprocal private letter obligations. | `ExchangeCard` | `/api/exchange`, `/api/posts` | `Exchange`, `Post` | live | planned | planned | behind | Native should make this a primary dashboard card. | v1 | Join/cancel exchange, match, attach sent letters to exchange. | Compose context needed. |
| requests.board | Requests | User pins a handwritten ask or answers one addressed to them. | `/requests`, `/post/new?request_of`, `/post/new?fulfill` | `/api/requests`, `/api/requests/:id`, `/api/posts` | `LetterRequest`, `Post` | live | planned | planned | behind | Native routes ask/answer through reusable compose context. | v1 | Open/mine/for-me filters, answer only by requestee, withdraw by requester. | Compose state machine needed. |
| robins.round | Round Robins | Users contribute hidden sections to a collaborative letter. | `/robins` | `/api/robins`, `/api/robins/:id/sections` | `RoundRobin`, `RoundRobinSection`, `Post` | live | not started | not started | deferred | Native tablet canvas is ideal; defer until core composer is stable. | v1 | Start robin, add section, peek previous sliver, finish final post. | Wide canvas requirements. |
| dead-letters.claim | Dead Letters | User writes to no one or claims a sight-unseen letter. | `/dead-letters` | `/api/dead-letters`, `/api/posts` | `Post.isDeadLetter` | live | planned | planned | behind | Preserve ritual tone; native can use a physical mailbag metaphor. | v1 | Count, claim, open claimed letter, write dead letter, debt reminder. | Compose context needed. |
| groups.circles | Circles | User browses/creates hashtag-based circles and sees matching letters. | `/groups`, `/groups/[id]` | `/api/groups`, `/api/groups/:id/posts`, `/api/posts?groupId` | `Group`, `Post.ocrHashtags` | live | not started | not started | behind | Native can start with list/detail. | v1 | Create/delete group, filter posts by tag literals, paginate. | Group creation includes typed metadata. |
| stamps.book | Stamps | User views stamp balance/book and stamps posts. | `/stamps`, `StampButton` | `/api/stamps/balance`, `/api/posts/:id/stamp` | `Stamp`, `StampDesign`, `PostInteraction`, `User.stampBalance` | live | not started | not started | behind | Native stamp button should feel tactile but use same balance semantics. | v1 | Balance loads, post stamp toggles atomically, counts update. | Stamps are overloaded; document semantics. |
| moderation.review | Moderation | Hidden/review/deleted/private content does not leak. | Feed/detail/image routes | `/api/posts`, `/api/posts/:id`, `/api/images/:id`, `/api/media/comments/:id`, `/api/media/scratches/:id`, `/api/media/guestbook/:id` | `Post.needsReview`, `Post.deletedAt`, `Post.isPrivate` | partial | in progress | in progress | in progress | None. | v1 | Access rules match across clients and image proxy cannot leak private letters. | Shared `canViewPost` protects detail, images, comments, scratches, stamps, and profile post lists. Comment, scratch, and guestbook media proxy routes are in place. Production bucket policy audit remains. |
