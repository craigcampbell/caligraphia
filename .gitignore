node_modules
.next
.env
*.log
tsconfig.tsbuildinfo
# Tesseract language data — downloaded at runtime, never committed
*.traineddata

# Android local build state
android/.gradle/
android/**/build/
android/local.properties

# Host-written runtime stats for the admin panel
.runtime/
